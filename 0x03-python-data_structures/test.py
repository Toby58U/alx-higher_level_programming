#!/usr/bin/python3

def new_in_list(my_list, idx, element):

    original_list = [1, 2, 3, 4, 5]
    index_to_replace = 2
    new_element = 10

    result = new_in_list(original_list, index_to_replace, new_element)

    print("Original List:", original_list)
    print("Resulting List:", result)
